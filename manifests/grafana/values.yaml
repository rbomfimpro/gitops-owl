extraObjects:
- apiVersion: gateway.networking.k8s.io/v1
  kind: HTTPRoute
  metadata:
    name: grafana
  spec:
    parentRefs:
      - name: traefik-gateway
        namespace: traefik
    hostnames:
      - "grafana.owl.localhost"
    rules:
      - matches:
          - path:
              type: PathPrefix
              value: /
        backendRefs:
          - name: grafana
            port: 80